import { defineMessages } from "react-intl";

export const scope = "containers.Profile";

export default defineMessages({
  startProjectHeader: {
    id: `${scope}.start_project.header`,
    defaultMessage: "Profile message",
  },
});
